{
  "name": "bmacw",
  "productName": "BMacW",
  "version": "0.1.4",
  "description": "BMacW, native macOS BMW diagnostics (Chromium shell over the EDIABAS engine)",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build:server": "dotnet publish ../src/InpaMac.Server -c Release -o ./server-dist",
    "build:faultdb": "node ../scripts/build-faultdb.mjs",
    "check:faults": "node ../scripts/check-faults.mjs"
  },
  "devDependencies": {
    "electron": "^33.0.0"
  }
}
